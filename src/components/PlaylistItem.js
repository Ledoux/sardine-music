import React from 'react'

const PlaylistItem = ({ hit }) => {
  console.log('hit', hit)
  return (
    <div className="playlist-item">
      playlist
      <div className="sep" />
    </div>
  );
};

export default PlaylistItem
